package roleplay.items.weapons.meleeWeapons;

public class Club extends MeleeWeapons {
    public Club(int weight, String designation, int prise, int attackValue, int defenseValue) {
        super(weight, designation, prise, attackValue, defenseValue);
    }
}
