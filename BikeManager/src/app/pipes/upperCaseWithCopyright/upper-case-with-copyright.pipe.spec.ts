import { UpperCaseWithCopyrightPipe } from './upper-case-with-copyright.pipe';

describe('UpperCaseWithCopyrightPipe', () => {
  it('create an instance', () => {
    const pipe = new UpperCaseWithCopyrightPipe();
    expect(pipe).toBeTruthy();
  });
});
